grp0: "GP0"                 # set group name for group 0
proc_ctrl add0              # select cell and add area to group 0

proc_dp/proc_core/proc_p1 add1:"GP1"             # select cell and add area to group 1 (with group naming)
proc_dp/proc_core/proc_p1/clk_gate_p1a_reg sub1  # select cell and sub area from group 1

